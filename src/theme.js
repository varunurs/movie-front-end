import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#000",
      blue: "#00f",
      rating: "#5d1ce8",
      genre: "#d90b56",
      duration: "#b3b3b3",
    },
    text: {
      primary: "#000",
      white: "#fff",
    },
  },
});

export default theme;
